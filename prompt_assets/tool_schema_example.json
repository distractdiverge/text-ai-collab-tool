{
    "$schema":"https://json-schema.org/draft/2020-12/schema",
    "properties": { 
        "Text":{
            "type": "string",
            "description": "The plain-text content (≤ 10 000 characters) to be analysed."
        },
        "StyleGuide": { 
            "type": "enum", 
            "description": "Which style guide to validate against. Leave blank for a general academic mix.",
            "enum": ["APA", "Chicago", "MLA", "IEEE", "None"]
        },
        "SeverityFilter":{ 
            "type": "enum",
            "description": "Limit results by severity.",
            "enum": ["all", "warning", "error"]
        }
    },
    "additionalProperties": false,
    "type": "object",
    "required":[ "Text"]
}